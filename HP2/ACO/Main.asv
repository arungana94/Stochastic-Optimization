cityLocation = LoadCityLocations;
nCities = size(cityLocation,1);
vis = GetVisibility(cityLocation);
pher = InitializePheromoneLevels(nCities,0.1);
%%
path = GeneratePath(pher,vis,1,3);
pathLength = GetPathLength(path,cityLocation);
delpher = ComputeDeltaPheromoneLevels(pathCollection)
%path = randperm(nCities);                
%tspFigure = InitializeTspPlot(cityLocation,[0 20 0 20]); 
%connection = InitializeConnections(cityLocation); 
%PlotPath(connection,cityLocation,path);     